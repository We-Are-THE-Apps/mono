<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Astro description" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>index</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Albert+Sans:wght@200..800&display=swap"
            rel="stylesheet"
        />
        <script>
            var uIp;
            fetch("https://api.ipify.org?format=json")
                .then((response) => response.json())
                .then((data) => {
                    console.log(data.ip);
                    uIp = data.ip;

                    if (uIp != "85.106.97.31") {
                        alert("Access denied.");
                        setInterval(() => {
                            alert("Access denied.");
                        }, 3000);
                    }
                })
                .catch((error) => {
                    console.log("Error:", error);
                });
        </script>
    </head>
    <body
        class="bg-gradient-to-br pl-10 flex-col gap-0 text-white flex justify-center items-start from-slate-950 to-teal-950 w-screen h-screen"
    >
        <div class="flex flex-row opacity-80 justify-center items-center text-3xl gap-2">
            <span class="text-4xl font-black">mono</span>
            by
                <img
                class="size-12 aspect-square"
                src="https://theapps.vercel.app/logoWHITEFG.svg"
                alt="THE logo"
            />
        </div>
        
        <h1 class="text-9xl font-black text-teal-200">TALK. DISCUSS. <br /> GET DONE.</h1>
        <p class="text-2xl">
            Welcome to a new and better way to messaging with your community. Stay tuned.
        </p>
    </body>
</html>
